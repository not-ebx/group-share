mutation Register(
    $username: String!,
    $email: String!,
    $password: String!
){
    register(userdata: {username: $username, email:$email, password: $password}){
        ...userResponse
    }
}